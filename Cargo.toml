[package]
name = "willos-graveyard"
version = "0.3.0"
authors = ["TLovell <trevorlovelldesign@gmail.com>"]
edition = "2021"

[features]
# For release builds, make sure to use --no-default-features
default = ["hot", "fast-compile"]
dev = ["inspector"]

hot = []
fast-compile = ["bevy/dynamic"]
inspector = ["bevy-inspector-egui"]

[dependencies]
bevy = {version = "0.8", features = ["wav"]}
bevy_easings = "0.8"
bevy_ecs_ldtk = { git = "https://github.com/Trouv/bevy_ecs_ldtk", branch = "feat/respawn" }
rand = "0.8"
serde = "1.0"

bevy-inspector-egui = { version = "0.12", optional = true }

[target.wasm32-unknown-unknown.dependencies]
bevy_ecs_ldtk = { git = "https://github.com/Trouv/bevy_ecs_ldtk", branch = "feat/respawn", features = ["atlas"] }

